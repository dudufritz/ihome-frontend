{
  "$schema": "https://json.schemastore.org/staticwebapp.config.json",

  "//": "SPA fallback — o React Router resolve as rotas no navegador, então",
  "//2": "qualquer caminho que não seja um arquivo real precisa devolver o",
  "//3": "index.html. Sem isto, dar F5 em /auditoria retornaria 404 do servidor.",
  "navigationFallback": {
    "rewrite": "/index.html",
    "exclude": ["/static/*", "*.{png,jpg,jpeg,svg,ico,webmanifest,json,js,css,txt}"]
  },

  "//4": "Cabeçalhos de segurança aplicados a todas as respostas.",
  "//5": "microphone=(self): o assistente por voz usa SpeechRecognition, que o",
  "//6": "navegador recusa quando a política nega o microfone. Negar tudo por",
  "//7": "padrão é a postura certa, mas negar o que o próprio app precisa é",
  "//8": "apenas quebrar a funcionalidade. (self) libera só esta origem —",
  "//9": "um iframe de terceiro continua sem acesso.",
  "globalHeaders": {
    "X-Content-Type-Options": "nosniff",
    "X-Frame-Options": "DENY",
    "Referrer-Policy": "strict-origin-when-cross-origin",
    "Permissions-Policy": "geolocation=(), microphone=(self), camera=()",
    "Strict-Transport-Security": "max-age=31536000; includeSubDomains"
  },

  "//5": "O Service Worker precisa ser revalidado sempre, senão o navegador",
  "//6": "serve a versão antiga do app indefinidamente após um deploy.",
  "routes": [
    {
      "route": "/service-worker.js",
      "headers": { "Cache-Control": "no-cache, no-store, must-revalidate" }
    },
    {
      "route": "/static/*",
      "headers": { "Cache-Control": "public, max-age=31536000, immutable" }
    }
  ],

  "mimeTypes": {
    ".json": "application/json",
    ".webmanifest": "application/manifest+json"
  }
}
